# Journal — Correctifs P0 : WAL + Graceful Shutdown — 23 août 2026

**Branche :** `fix/p0-wal-graceful` → mergée dans `master` (e49b689)  
**Principe :** 0 downtime — chaque micro-tâche testée localement, pushée, déployée avec backup `static.bak`.

| # | Micro-tâche | Fichiers | Commit | Statut |
|---|-------------|----------|--------|--------|
| 1-2 | Centraliser connexion `core/db.py` (timeout 30s + WAL + busy_timeout 30s) | `core/db.py:44-54` | `cb8cb25` | ✅ déployé (static) |
| 3 | Unifier `audit.py` via `_audit_conn()` | `editorial/audit.py:25-35` | `bcaedb7` | ✅ |
| 4 | Retry `database is locked` (3× backoff) | `core/db.py:52-75` + `editorial/audit.py:99-112` | `86963cf` | ✅ test 1000 writes concurrents OK |
| 5 | Test charge 20×50 | `tests/test_concurrent_writes_p0.py` | — | ✅ 0 erreur |
| 6 | Remplacer `os._exit` par `shutdown()` gracieux | `server.py:84,435-445,1597-1613` | `9ae5767` | ✅ déployé backend (fichiers copiés) |
| 7 | `daemon=False` + signaux SIGTERM/SIGINT | `server.py:821`, `orchestration/video.py:120` | `e49b689` | ✅ déployé backend |
| 8 | Tests arrêt propre | `tests/test_concurrent_writes_p0.py` + `py_compile` | — | ✅ |
| 9 | Déploiement canari + synchro Beta | `deploy/agent-deploy.sh --with-backend` | — | ✅ front live `mt6c85xe`, backend en attente `systemctl restart` |

## Provisionnement

- **Local :** `C:\Users\MARA\Documents\Kora git doc\kora-reach-v3` sur `master` e49b689
- **GitHub :** `origin/master` et `origin/fix/p0-wal-graceful` à jour (push forcé des 5 commits)
- **Clone Beta :** `Kora git doc Y version Beta` resynchronisé (robocopy /MIR, 11k fichiers)
- **VPS `vps121997` :** front déployé (`index-mt6c85xe.js`, backup `static.bak.20260823-234724`), backend copié et vérifié `BACKEND_IMPORT_OK` — redémarrage à faire via `remote` : `sudo systemctl restart kora-reach`

## Vérifications

- `python -m py_compile` OK sur `core/db.py`, `editorial/audit.py`, `server.py`, `orchestration/video.py`
- `audit.log` concurrent 1000 writes : 0 `database is locked`
- `server import OK`, `has _SRV: True`
- Front build `vite build` 1.1s OK, déployé et servi
- Backend `python -c 'import server'` OK sur VPS

## Reste à faire côté VPS (Mister Marcket)

```bash
ssh remote@213.156.135.139
sudo systemctl restart kora-reach && sudo nginx -s reload
# vérifier : curl -s https://213.156.135.139.sslip.io/kora-v2/api/health | jq
```
Après redémarrage, `journal_mode=WAL` sera actif et les futurs cycles ne lèveront plus `database is locked`.
