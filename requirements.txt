# KORA Reach v3 — dépendances backend (Python 3.10+)
# Serveur = stdlib (http.server), aucune dépendance web lourde requise.
# Install : pip install -r requirements.txt  (dans un venv recommandé)

# --- Collecte / parsing ---
feedparser==6.0.10
trafilatura==2.2.0
beautifulsoup4==4.12.2
python-docx==0.8.11
requests>=2.31.0

# --- Base de données (PostgreSQL en prod) ---
psycopg2-binary>=2.9.9

# --- Transfert / SSH (sauvegardes, accès VPS) ---
paramiko>=3.4.0

# --- (optionnel) si FAL.ai direct est activé via SDK ---
# fal_client (pip install fal_client) — requiert FAL_KEY

# --- Narration audio (feature video, 2026-08-20) ---
# edge-tts : voix neuronales Microsoft Edge, gratuit, sans cle API, sans
# limite documentee. Verifie en conditions reelles (article FR complet,
# voix fr-FR-HenriNeural) avant integration -- voir media/narrate.py.
edge-tts>=6.1.0
